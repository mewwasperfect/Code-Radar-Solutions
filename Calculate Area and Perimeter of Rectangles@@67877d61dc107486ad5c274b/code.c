#include <stdio.h>

int Area(int a){

}

int main(int r){
    scanf("%d" , &r);
    for(int R = 1 ; R <= r ; R++){
        printf("Rectangle %d: Area = %.2f, Perimeter = %.2f\n" , R);
    }
    return 0 ;
}